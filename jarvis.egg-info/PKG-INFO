Metadata-Version: 2.4
Name: jarvis
Version: 0.1.0
Summary: Local AI desktop assistant foundation
Requires-Python: >=3.12
Description-Content-Type: text/markdown
Requires-Dist: python-dotenv>=1.0.1
Requires-Dist: pydantic>=2.7.4
Requires-Dist: httpx>=0.27.0
Requires-Dist: playwright>=1.46.0
Requires-Dist: pywinauto>=0.6.8
Requires-Dist: pyautogui>=0.9.54
Requires-Dist: Pillow>=10.4.0
Requires-Dist: spotipy>=2.24.0
Provides-Extra: dev
Requires-Dist: pytest>=8.3.2; extra == "dev"
Requires-Dist: pytest-asyncio>=0.23.8; extra == "dev"
Requires-Dist: ruff>=0.6.1; extra == "dev"
Requires-Dist: mypy>=1.11.1; extra == "dev"

# Jarvis V0.1

Jarvis is a local AI desktop assistant foundation designed for future computer-use automation, browser orchestration, Spotify control, media handling, and local tooling.

This repository is intentionally focused on a clean architecture and a working CLI shell. It does not yet implement autonomous computer control or a production-grade AI reasoning engine.

## Project status

Current milestone:

- Project foundation and package layout
- CLI and basic agent lifecycle
- Tool + registry abstraction
- Planner and executor skeletons
- Skill architecture for browser, computer, media, Spotify, and system
- Logging and configuration foundation
- Automated tests for core registry/agent behavior

## Architecture overview

The project is organized into a small set of reusable layers:

- `main.py` — interactive CLI entry point
- `jarvis/config.py` — environment and settings management
- `jarvis/core/` — agent, planner, executor, registry, and tool abstractions
- `jarvis/skills/` — domain-specific capability modules
- `jarvis/vision/` — screen and OCR-oriented future components
- `jarvis/voice/` — speech interfaces
- `jarvis/memory/` — future user/context memory layer
- `jarvis/utils/` — logging utilities

## Requirements

- Python >= 3.12
- Access to a local development environment
- Optional browser runtime for future Playwright automation

## Virtual environment setup

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

## Dependency installation

```powershell
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

## Playwright browser installation

Playwright requires browser binaries to be installed separately:

```powershell
playwright install chromium
```

The project will not auto-download browsers when you start the CLI.

## Environment setup

Copy the sample environment file and populate any values you need locally:

```powershell
Copy-Item .env.example .env
```

The repository intentionally keeps secrets out of source control.

## Running Jarvis

```powershell
python main.py
```

You will see a basic interactive CLI shell with commands such as:

- `help`
- `tools`
- `status`
- `exit`

## Running tests

```powershell
pytest
```

## Linting and type checking

```powershell
ruff check .
mypy jarvis
```

## High-level roadmap

```text
V0.1 - Core architecture + CLI
V0.2 - Reliable browser control
V0.3 - Spotify integration
V0.4 - Windows UI automation
V0.5 - Screen understanding / vision
V0.6 - LLM planner
V0.7 - Verification + recovery loop
V0.8 - Voice input/output
V0.9 - Memory and user preferences
V1.0 - Integrated local Jarvis assistant
```

## Notes

This foundation intentionally keeps the scope limited to clean architecture, registration, execution flow, and actionable shell startup. It does not include destructive computer control, unrestricted shell execution, or advanced autonomous behavior.
